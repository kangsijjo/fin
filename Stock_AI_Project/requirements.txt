# 정본 의존성 목록 (2026-09-04 부터). 전체 pip freeze 덤프는 requirements-lock.txt 로 이동 —
# 그 파일은 환경 재현 시 무관한 패키지 충돌로 설치가 깨지기 쉬워 정본으로 쓰지 않는다.
# ─────────────────────────────────────────────────────────────
# 모의투자 가동에 필요한 최소 의존성
#
# 제외한 것 (이유):
#   - torch / transformers / safetensors / tokenizers / huggingface_hub
#       FinBERT 감성분석은 현재 코드(make_features, indicators) 에서
#       news_sentiment=0.0 으로 하드코딩되어 실제 미사용.
#   - shap          : 예측 근거 설명용. 거래 자체에는 불필요.
#   - streamlit/plotly : 대시보드 전용. 매매에 불필요.
#   - numba/llvmlite   : pandas-ta-classic 은 의존하지 않음.
#
# 버전은 일부러 고정하지 않음:
#   전체 freeze 본(requirements-lock.txt)이 미래 버전(2026.x 등)으로 박혀 있어
#   매번 wheel 못 찾는 에러가 났음. 최신 호환 wheel 을 pip 가 선택.
# ─────────────────────────────────────────────────────────────

# 데이터 수집
finance-datareader
pykrx
pandas
numpy
requests
beautifulsoup4
lxml

# 기술지표 계산 (indicators.py 가 import pandas_ta_classic as ta)
pandas-ta-classic

# 머신러닝
lightgbm
scikit-learn

# 백테스트
backtrader
matplotlib
tqdm

# 환경/설정
python-dotenv
PyYAML

# 스케줄러 (scheduler.py)
schedule


streamlit>=1.30
plotly>=5.18